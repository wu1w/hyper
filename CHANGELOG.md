# Changelog

未发版之前以 git log 为准。下面记用户能看见的行为变化。

- Cursor hop 几何：`auto` 对 grok-4.6 映射 **high**（`/think xhigh` 仍是最高档）；Write/StrReplace 不再自动 `cargo check`，需要时用 ReadLints；同一跳里 Write 后面的 Read 会看到新文件；连续只读仍并行。10 次只读巡检后给 Grok 一条 `[channel]` 收口提醒（不卸工具、不 Stop）。旧 AGENT.md 里「编辑后自动 diagnostics」会升级。终稿里引用 `I'll write files` 或 Cursor citation 围栏不再当成泄漏 Write。`hyper --workspace <dir> web` 进控制台，不再把 `web` 当成 oneshot 提示词。短 `block_until_ms` 的 Shell 按 Cursor 后台交给 AwaitShell，不再硬 timeout。`block_until_ms=0` 立刻后台，不再当成「没有超时」空等到命令结束。`yes` 一类无界输出在活窗口满了之后停读、不掐管道，短 `block_until_ms` 能交给 AwaitShell，不再 SIGPIPE 成 exit -1。compact 之后 RAM 里的归档工具输出收成 stub，JSONL / recall(seq) 仍是原文；compact 前活窗口工具输出超过 4MiB 也 stub 最旧的，过夜不会把整份 jsonl 留在 RAM；`/fork` 和改 workspace 从磁盘重写，不会把 stub 写回。SwitchMode **ask** / `/clarify` 只读（含 plan.md）；同一跳 SwitchMode+Write 也拦。工具跳上的实质正文会记下 `last_spoken`，下一跳空了仍交那一段，不落「没有可见回复」。ReadLints 不会把 Cargo 包根上未编进 crate 的 stray `.rs` 报成「没有编译错误」。Write 跟随已有文件符号链接，原子 rename 不再把链接替换成普通文件。Read / StrReplace 把含 NUL 或非法 UTF-8 的文件当成二进制拒绝，不把 `\0` 当文本页。Write 到已有目录返回 `is a directory`，不再甩 OS error 21。把文件当目录写（`keep.txt/oops`）返回 `is not a directory`，不再甩 os error 17。Write / Delete / Read / Shell spawn / view / EditNotebook / TodoWrite / run_code 的 I/O 失败不再带 `os error N`。假图片（扩展名是 png 但字节不是）Read/view 报错，不再把垃圾塞进下一跳把整轮打成 HTTP 400。端点不能感知图片/视频时 view 是 Error，不把 cannot-perceive 笔记当成成功看过。远程图片 URL 先拉取再验魔数，404/HTML 不再交给 Grok 变成 `invalid_image`。Shell `working_directory` 不存在时直接说目录没有，不再 `os error 2`。Read 目录无权限返回 Error（带 permission denied，不带 os error N），不再假装空目录。Grep/Glob 指到不存在的 path 会 Error，不再假装「No matches」。Grep 指到含 NUL 的单文件返回 binary Error，不再假装 No matches；64KiB 之后的 NUL 若进了 rg 输出同样 Error。Grep walk 扫目录时跳过的二进制会脚注 skipped N binary file(s)。ripgrep 在目录全是二进制时同样脚注，不再只报 No matches。Grep 非法正则会把 ripgrep 的 stderr 当成 Error，不再吞掉后报「No matches」。Grep 指到无权限文件或目录返回 Error，不假装 No matches；目录里只有部分路径无权限时仍返回其它命中，不再把整次搜索报成 Error 丢掉 hits；ripgrep 超时也是 Error，不再静默改走 walk。Read `limit=0` 回落到默认页长，不再交一页空行。AwaitShell 等已失败的子任务是 Error，已取消是 Interrupted，不再假装 Success；`wait_commands_or_subagents` 带 `task_ids` 会等整组。Grep `head_limit=0` 按默认上限搜，不再假装 No matches。空的 Grep `pattern` / Glob `glob_pattern` 直接 Error，不再拿空串去扫全树。Glob 子目录无权限时不再假装 No files matching：全不可读 Error，部分不可读仍返回其它命中。`no glob_pattern` 这种词不会再被当成用户禁止了 Glob。假 `.mp4` / `.wav`（扩展名对、容器不对）`Read`/`view` 报 Error，远程视频/音频 URL 先拉取再验魔数。Glob 会列出文件符号链接（含断链）以及 FIFO 名字，不走进目录符号链接。Delete 删除符号链接本身，不跟随去删目标。Read / Write / StrReplace 碰到 FIFO/socket/设备返回 `not a regular file`，不再阻塞 hop。超过 8MiB 的文本 Read/StrReplace 直接 Error，不把整文件灌进内存。Write 的 `contents` 同样 8MiB 帽。`skills_auto_catalog` 系统提示最多 24 条（工作区技能优先），不把整份 ~/.cursor/skills 灌进每一跳。Grep / EditNotebook 碰到 FIFO 同样走非阻塞读。Grep 目录扫描 `--max-filesize 8M`（与文本 slurp 对齐），单个大文件仍不受该帽；不再把 1–8MiB 源码假装成 No matches；超长匹配行按 `--max-columns` 截断预览；rg 一边跑一边抽 stdout，避免管道撑满后 `wait` 死锁。walk 回落到单文件 Grep 时，二进制/非 UTF-8 返回 Error，不再假装 No matches；walk 扫目录时跳过的二进制会脚注 skipped N binary file(s)，不再只报 No matches。远程媒体与 GenerateImage 拉图按上限流式截断，不再无 Content-Length 时整包灌内存。下一跳内联本地图会跳过 FIFO。skill 的 SKILL.md 若是 FIFO/过大直接 Error，不阻塞 hop。开场读 AGENT.md / AGENTS.md / cron.json / MCP overlay 同样跳过 FIFO。会话 JSONL 若是 FIFO 直接 Error；整文件超过 64MiB 或单行超过 8MiB 拒绝加载，不再灌爆内存。列会话按行读。邻座 recap / 会话 inspect / blob GC / recall seq 打开 jsonl 走非阻塞读，JSONL 追加/重写/official sidecar 打开带 O_NONBLOCK，FIFO 换上也不卡 hop。 overnight 写入（CURRENT、心跳、IM cursor）走 `write_if_regular` 的非阻塞 open，FIFO 换上也不卡写端。IM / OAuth / probe / compact / LLM 错误体按上限抽流。`/diff` 与 git worktree 边抽 stdout。ffmpeg / whisper-cli 的 stdout/stderr 按 1MiB 抽流，转录文本截到 4000 字。Shell 在活窗口 1MiB 之后最多再丢弃 8MiB，截断时附 truncated 脚注，不再假装完整输出，避免 `yes` 一类无界输出把 CPU 吃满到 24h 墙。run_code stdout 同样 1MiB 后再丢弃 8MiB 并标明 truncated，含 NUL 时省略为 binary。git / rg / ffmpeg 的 cap drain 同样在再丢 8MiB 后松开管道。`--print` 收轮时取消仍在跑的后台 Shell。workset `git status` 与心跳 git 边抽 stdout。WebFetch 拒绝 `javascript:` / `data:` / `file://`。whisper 转录 JSON 按上限抽流。本地音频走 HTTP 转录前拒绝 FIFO，并按 8MiB 帽 Error，不整文件灌内存。Write 执行前读旧内容、GenerateImage dest、MCP downloadPath 同样跳过特殊文件和大文件。Glob 的 target_directory 若不是目录同样 Error。ComputerUse 无 Screen Recording 时立刻 Error，不再卡在 xcap；单跳 12 秒墙。`GenerateImage` filename / `FetchMcpResource` downloadPath / `EditNotebook` 走写围栏。未挂进 `tools[]` 的 `GetDynamicTools` / `CallDynamicTool` / `FetchMcpResource` / `mcp` / `memory_search` 幻觉调用返回 unknown。MCP 未配置时这三件套本身也是 Error，不再假装空目录成功。

- compact / memory 写入前 scrub 口令、token 与 `xai-` 长密钥；`block_until_ms` 接受 JSON 浮点/字符串，不再把 `1500.0` 当成没超时。`~/.grok-hyper` 下 config、sqlite、memory markdown 默认 0600。ComputerUse 在没有 Accessibility 时不再构造 Enigo，schema 标明截图与点击权限独立。turn panic 会写 step/run Error、脱敏诊断，并落到 desktop.log。心跳 `HEARTBEAT.md`、`web-cron.json`、IM `routes.json`、grok `auth.json` 遇到 FIFO 或超大文件直接跳过或 Error，不再挡住 `hyper web` 脉冲或 login。工作区 pulse 读 HEARTBEAT.md 同样跳过 FIFO，不再卡过夜心跳。`config.toml` 过大时 `load_or_init` 拒绝加载。超过 8MiB 的 PDF/Office 走文档抽取上限（96MiB），不再被文本 Read 的 8MiB 帽误拦。邻座会话 recap 只读 jsonl 头尾，不再整文件灌内存。打开会话 JSONL 超过 64MiB 或单行超过 8MiB 直接 Error；进行中的 append 同样拒绝超过 64MiB，不再整包灌 RAM。会话列表 inspect 只读 jsonl 前 512KiB。官方 compact sidecar、session JSONL 重写、子代理 `{id}.task.json`、doc-cache、home 私密写入、desktop.log / feishu.log 碰到 FIFO 直接 Error 或跳过，不再卡 compact / AwaitShell / 过夜 panic。官方 `*.official.json` 按 32MiB 上限加载，不再被 8MiB 文本帽误丢。vendor tokenizer / chat template 若是 FIFO 或超过 64MiB 直接 Error，不卡启动校验。无 ffmpeg 时 WAV 直拷按 8MiB 帽。BlobStore 不把 FIFO 当成已有 blob。Search 索引文件上限与文本 slurp 对齐 8MiB，1–8MiB 源码不再从 Search 里消失。Search 指到 home/Desktop/盘符根直接 Error，不再假装 still warming；空索引是 No matches，构建失败是 Error。WebFetch 超 fetch_max_bytes、Grep rg 管道满 512KiB 附 truncated 脚注。view 读本地图/音视频与 ffmpeg 抽帧走非阻塞读。IM routes.json 打开与 flush 同样非阻塞、8MiB 帽。TodoWrite / CURRENT / chat template 不再 `read_to_string` 直灌。官方 compact sidecar（32MiB）、vendor 校验（64MiB）、IM 本地附件（16MiB）走同一套带帽非阻塞读。Telegram offset / 微信 cursor / 飞书 catchup / pairing overlay 写入跳过 FIFO，不卡过夜脉冲。`cron.json` 保存同样跳过 FIFO。Office zip 条目 8MiB、合计 16MiB 抽取帽，不再被 zip bomb 灌爆内存。PDF 抽取输入帽 16MiB、抽出文本 8MiB、页对象超过 200 直接 Error，不先让 pdf_extract 扩内存。Shell `cat`/`head`/`tail` 指到 FIFO（含 `cat < fifo`）；`echo > fifo` / `dd if=` / `cp fifo` / `cat fifo | wc` / `wc fifo` 同样立刻 Error立刻 Error，不再卡到 24h 墙。GenerateImage 落地拷贝 16MiB（不再被 8MiB 文本帽误拦）。会话 blob GC 可扫到 64MiB jsonl。run_code 脚本、MCP downloadPath、imagine 落盘跳过 FIFO。probe.json / inbox 落盘同样跳过 FIFO。
- 瞬时重试从第一次失败起算 10 分钟，长 hop 超时后仍能再试；429 认 Retry-After、额度耗尽不空转。JSONL 只修撕尾、中间坏行整段拒绝。会话写失败后不再继续跑后续工具。QQ / 飞书 / 企微 / 钉钉断线按健康时长退避。输出 token 按剩余窗口收紧，不因此收轮。恢复未配对的 tool_calls 会标成 Interrupted。MCP 按响应 id 配对并限制帧长。
- 同参工具第 6 次把 `[channel]` 提醒塞进工具 hop，Grok 看得到；低精度第 2 次提醒、仍不 halt。官方 blob 只写 sidecar，不进 JSONL，重启按 skip 接上。HEARTBEAT.md 站桩不空转；显式 `/loop` prompt 仍按间隔跑。空闲读超时样例为 0。过夜迁墙写回前备份带注释的 `config.toml.orig`。
- 自改：`docs/dsh-playbook.md` + 仓库短 `AGENTS.md`。`./scripts/self-snap.sh` 打 tag；`hyper-self-snapshot.sh` / `hyper-self-rollback.sh` 备份家目录配置。`/undo` 只回会话。
- 过夜：加载配置时把旧默认 500 步 / 1800 秒墙改成 0 并写回。HTTP 不再用整段请求 30 分钟墙，只对空闲读超时。思考截断最多续 3 跳，不立刻 Exhausted。心跳不认目录 mtime。子 agent general/office 不限步。AwaitShell 默认 120 秒。
- 过夜 compact：官方 blob 按 blob+后缀计价；含 Write 的最后一轮工具不归档；本地卡片 12000 字；TodoWrite 进 Current State；官方 blob 落到会话旁 `*.official.json`，重启可接上。
- 长任务默认不限跳数、不限墙钟（`max_steps` / `max_wall_seconds` = 0）。同参工具只提醒一次，不再 `budget:repeat` 静默停。硬窗口只 compact 不收轮。
- 单一裁决：成功工具结果不改写、不跳过 Read/Grep。AwaitShell 超时为 Interrupted。官方 compact 只跟 soft 窗口。心跳先 pulse，工作区没变不调模型。混合跳先并行只读，串行段认 `/stop`。
- 跟进轮不再只因为工具次数就压缩；Grok 路径不再把相近 Grep / Read / Search 劝退成空结果。`recall` 开场就挂、压缩也不卸。一批改完才跑一次诊断，成功也会回传。官方压缩先看全文，本地重写会清掉旧 blob。
- Grok Responses 收得到收口/催 Write 备注（`[channel]`）；进度旁白不再当成用户气泡，空跳兜底会作为 assistant 落下。Chat Completions 的 `finish_reason=length` 与 Responses `incomplete`/`failed` 按截断/失败处理；长度截断计数在干净跳后清零。
- 停止权收拢到单裁决器：撞思考帽保留残稿并失败截断工具，无 finish_reason 的断流当瞬时错误重试；无工具的进度旁白不当终稿。Thinking 保持开启（Cursor / grok CLI），不走 27B thinking-off。
- 跟进轮对齐 Cursor / grok CLI：compact 优先留下 Write/StrReplace 过的文件，不再只留 Glob/Read 头尾；`[history]` 默认只带本场归档，邻座会话要用户问起才贴；控制台撞步数/时间墙也留一跳收口，方便下一轮接着写。
- 巡检门对齐 Cursor / grok CLI：`tools[]` 中途不卸。连续只读不再跳过、不改写结果。泄露旁白（空 html 围栏、I'll write files）不当终稿，只催一次原生 Write；「已用 Write 工具写入」仍是终稿。步数墙才是硬停。
- 空跳不再当成功结束：无正文、无工具、无 usage 先重试一次；仍空则收口一跳，再空则给「没有可见回复」兜底（控制台也会，不再交空白气泡）
- Glob / Shell 只拦工作区根上的无过滤递归扫；`**/*` 给一份顶层样本（跳过 vendor / 嵌套 `release` / `out` / `unpacked`），不再空手训一通。带扩展名的会真扫，满 200 条截断；`target_directory` 指到文件时直接报错
- Grep 默认不扫打包目录（显式 `path` 进该目录仍可搜）。重复 / 近义 pattern 会折；目录级风暴上限 12 次，与 Search 是否挂上无关；单文件 Grep 不占名额
- 连续只读不再跳过、不改写结果。泄露的 Write 旁白只催一次原生工具，不禁止继续 Read。步数墙才是硬停
- 思考撞上限：软提醒后按原推理模式给一次更宽重试，不关 thinking。空正文先收口一跳（工具仍在），再空则「没有可见回复」
- ReadLints：超时 / 没跑 checker 不再报「没有编译错误」；Incomplete 标 Error（正文仍说明不是编译结论），避免模型把 Success 当成干净。整树 `git diff` / `find` / `ls -R` 跳过时标 Error（附 hint），不再假装命令成功。工作区根上未过滤的 Glob `**/*` 标 Error（附顶层 sample），不再假装整树列全。PDF `/Length` 声明超过摘录帽时在 pdf_extract 前 Error。 Shell `grep`/`rg`/`vim`/`nano` 打开 FIFO 在 spawn 前 Error。 OAuth `auth.json.tmp` FIFO 用 write_if_regular，不再卡住 persist。 Shell `curl`/`wget` `file://` FIFO 在 spawn 前 Error。 Shell 固定 `PAGER=cat`/`GIT_PAGER=cat`，`git log` 不再挂 less。 PDF 间接 `/Length N 0 R` 会解析 `N 0 obj` 整数，挡 flate 炸弹。 `sqlite3`/`createReadStream` 打开 FIFO 同样预检。`os.system`/`subprocess`+`listdir` 碰到 cwd FIFO 在 spawn 前 Error；旁边 `echo hi` 仍可跑。Grep/Glob walk 扫到时间预算标 Error，空结果不再假装 No matches。python3 -c / run_code 里引号路径若是 FIFO 立刻 Error。xlsx `dimension` 超 25 万格在 calamine 扩 Range 前 Error。`.xls` / `.xlsb` 一律 Error（没有格子预检，禁止进 calamine）。无 `/Length` 的 `/FlateDecode` PDF 在 pdf_extract 前 Error。 只有 FIFO 的目录上 Grep 标 Error，不再假装 No matches。 目录扫描因 8MiB 帽漏掉超大文件时 Grep 标 Error，不再假装 No matches。 解释器 `open('huge.txt')` 超过 8MiB 在 spawn 前 Error，不让子进程整文件灌 RAM。 Glob 命中顶到 200 条、Read 目录列出截断，都标 Error，不再假装列全。 WebFetch 碰到体积帽截断标 Error，不再假装看完整页。 Web 客户端把 127.0.0.1/localhost 留在 NO_PROXY，避免 WebFetch 本机地址被公司代理吸走。 Grep ripgrep 管道顶到 512KiB 标 Error，不再假装搜全。 Grep 旁边有超大文件时，即使小文件命中也标 Error（incomplete），不再假装搜全。 Grep 旁边有 FIFO 时，即使小文件命中也标 Error（incomplete），不再假装搜全。 PDF `/FlateDecode` 在 pdf_extract 前按 8MiB 帽试解压，挡 Length 合法的 zip bomb。 Shell / run_code stdout 顶到 1MiB 活窗标 Error，不再假装命令输出看全。 tarfile.open / cv2.VideoCapture / h5py.File 打开 FIFO 在 spawn 前 Error。 pandas.read_* / shutil.copy 打开 FIFO 在 spawn 前 Error。 WebSearch 引擎页截断标 Error。 Shell `deno eval`/`bun -e`/`ruby File.binread` 打开 FIFO 在 spawn 前 Error。 WebFetch 碰到体积帽截断标 Error，不再假装看完整页。python3 heredoc / `p='fifo'` / `gzip`/`strings` 指到 FIFO 立刻 Error；ODS `number-rows-repeated` 炸弹在 calamine 前 Error。Shell `awk`/`sed`/`jq` 指到 FIFO 同样立刻 Error。xlsx `dimension` 写小、实际 `<c` 很多时按较大值拦截。xlsx 超过 80 张表在 calamine 前 Error。pptx 超过 80 张幻灯片直接 Error。doc-cache 写入走 write_if_regular。Shell `tar`/`zip`/`rsync` 指到 FIFO 立刻 Error。Shell `diff`/`cmp`/`comm` 指到 FIFO 立刻 Error。python/node `open(p)` / `os.listdir` 且 cwd 有 FIFO 时立刻 Error；`open('notes.md')` 仍可跑。Shell `ffmpeg`/`sqlite3`/`openssl` 指到 FIFO 立刻 Error。tsc 从文件向上找最近的 tsconfig.json（例如 `web/console`）
- `view` 未挂进 tools[] 时幻觉调用返回 unknown；Read 图片/音视频会直接加载，不再指向未挂载的 view
- `ComputerUse` 未进冻结 17（`features.computer_use` 未开）时幻觉调用返回 unknown，不再真去控桌面；Ask 也不会先弹审批
- Write schema 要求 `contents`；EditNotebook schema 要求 `target_notebook`
- TodoWrite / ReadLints 标为可并行，不再把同一跳后面的 Read/Grep 拖成串行
- `auto` 对 grok-4.6 的文档改为 **xhigh**（与 `Effort::auto_for` 一致）
- 频道页只列出进程内适配器（webhook / Telegram / QQ / 微信 / 企微 / 钉钉 / 飞书）
- Electron：`hyper web` 退出后退避再拉起
- WS `Lagged` 的 `resync` 带当前会话 events，直播不再先缺一截
- QQ「还在处理中」在回合结束后立刻停
- 控制台「文件」页修回 `../` 路径穿越防护（读围栏放开后 `safe_join` 误走未受限的 `resolve`，读写都可逃逸目录根）
- IM 长回复不再截断：QQ / Telegram / 微信 / 企微 / 钉钉 / 飞书按自然边界分成多条气泡，代码块跨段自动闭合、下一泡重开
- IM 入站防抖按渠道分档（微信 3s、QQ 1s、飞书 / Telegram / 企微 / 钉钉 0.6s、飞书带媒体 0.8s），手机上连发多条不再被切成两轮
- IM 正文流式预览：回答生成期间，飞书 / Telegram / 企微的进度气泡同步「回复中」草稿（12s 节奏），QQ / 微信 / 钉钉随攒批消息发出
- 整段回复 `NO_REPLY` / `SILENT` 时不外发（Hermes 沉默 token）；群聊消息带 `[发言者]` 前缀进上下文，模型能分清谁在说话
- `/stop`、`/steer`、`/queue` 等斜杠指令不再走入站防抖，立即到达会话；同窗口「帮我改」+ /stop 不会再被 merge 成一段正文
- 沉默 token 不再从「回复中」预览漏出：流式期间命中 NO_REPLY / SILENT 前缀就压住草稿段，终稿拦截也认 `NO_REPLY。` 这类尾标点
- 飞书 / Telegram / 企微终稿发出后，旧进度泡收敛回工具摘要（没走工具则收回 ACK），长回答不再上下看两遍；QQ / 微信 / 钉钉的短回答不再提前播「回复中」草稿（≥400 字才播）
- 微信群消息以 @ 开头、企微群消息被 @ 时标注 is_mentioned，默认 mention 策略下机器人不再装死；微信长文拆成多次 sendmessage 真分泡；QQ 媒体兜底不再复用已用掉的被动 msg_id；企微只回图时不再发「…」
- 中文长回答按句读（。！？；，、：）切泡，不再从无空格句子中间硬断；正文里行内提到 ``` 不再被误判成围栏开闭
- IM 卡的定位指引改为 Grep（默认冻结 17 没有 Search），不再教模型幻觉调用未挂载的工具
- Read 目录：先排序再截断；坏 outbox JSON 进 quarantine；频道 poll lock 显示「重试中」
- IM 群聊会话改为 per-user（可带 Telegram topic / 飞书 thread）；运行中 `/stop` `/approvals` `/plan` 等只有发起者能改
- `hyper web` 各 IM endpoint 共用一个 SessionRouter / ChannelManager，跨平台 resume 同一 JSONL 不会跑两套 agent
- AskQuestion / 审批按 prompt id 排队（FIFO、15 分钟 TTL）；过期按钮会提示，飞书 / Telegram 选完后收回卡片
- IM 增加 `/model` `/compact` `/undo` `/usage`；`/new <title>` 会真正写入会话标题
- IM `/background`（`/bg` `/btw`）把当前任务留在原会话继续跑，本聊天切到新会话；要停后台：`/resume <id>` 再 `/stop`
- AskQuestion / 审批卡片选完后标明选择者，并保留原问题
- QQ 键盘、钉钉 actionCard、企微 template_card、webhook `choices` 走原生按钮（微信 iLink 仍用序号回复）
